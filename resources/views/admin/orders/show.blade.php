@extends('layouts.admin')
@section('title', 'Order '.$order->order_number)
@section('content')
<section class="admin-page"><div class="page-heading"><div><p class="eyebrow">Order</p><h1>{{ $order->order_number }}</h1></div></div><div class="admin-form-card"><p><strong>{{ $order->user->name }}</strong><br>{{ $order->user->email }}<br>Payment: {{ $order->payment_method === 'online' ? 'Online Payment' : 'Cash on Delivery' }} ({{ ucfirst($order->payment_status) }})<br>Subtotal: Rs {{ number_format($order->subtotal, 2) }} · Shipping: {{ $order->shipping_charge > 0 ? 'Rs '.number_format($order->shipping_charge, 2) : 'FREE' }}<br>Delivery: {{ $order->delivery_estimate }}<br>{{ $order->shipping_name }} · {{ $order->shipping_phone }}<br>{{ $order->shipping_address }}</p><form method="POST" action="{{ route('admin.orders.update', $order) }}" class="inline-form">@csrf @method('PATCH')<label>Status<select name="status">@foreach (App\Models\Order::STATUSES as $status) @if ($status !== 'cancelled' || in_array($order->status, ['pending', 'confirmed', 'processing', 'cancelled'], true))<option value="{{ $status }}" @selected($order->status === $status)>{{ ucfirst($status) }}</option>@endif @endforeach</select></label><button class="btn btn--primary" type="submit">Update status</button></form></div><div class="table-wrap"><table class="admin-table"><thead><tr><th>Product</th><th>Qty</th><th>Unit price</th><th>Total</th></tr></thead><tbody>@foreach ($order->items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->quantity }}</td><td>Rs {{ number_format($item->unit_price, 2) }}</td><td>Rs {{ number_format($item->line_total, 2) }}</td></tr>@endforeach</tbody></table></div><h2>Total: Rs {{ number_format($order->total, 2) }}</h2></section>
@endsection